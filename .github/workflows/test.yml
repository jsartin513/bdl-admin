name: Tests

on:
  pull_request:
    branches: [preview, main]
  push:
    branches: [preview, main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'


      - name: Checkout bdl-packages
        uses: actions/checkout@v4
        with:
          repository: jsartin513/bdl-packages
          path: .vendor/bdl-packages
          token: ${{ secrets.BDL_PACKAGES_READ_TOKEN }}

      - name: Install dependencies
        env:
          BDL_PACKAGES_LOCAL_PATH: ${{ github.workspace }}/.vendor/bdl-packages
          BDL_PACKAGES_READ_TOKEN: ${{ secrets.BDL_PACKAGES_READ_TOKEN }}
        run: bash scripts/with-bdl-git-auth.sh pnpm install --frozen-lockfile

      - name: Run linter
        run: npm run lint

      - name: Run tests
        run: npm run test:run

      - name: Build project
        run: npm run build
